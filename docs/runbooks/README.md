# Runbooks — 9router-mw

Operational procedures for production gateway.

| Runbook | Purpose |
| ------- | ------- |
| (TBD) deploy | Deploy / restart workers |
| (TBD) rollback | Rollback to previous tag |
| (TBD) redis | Redis 6381 health / failover notes |
| (TBD) incident | Common failure modes |

Domain: `router.budgezen.com`  
Listen: `127.0.0.1:20128` (systemd User=router)  
Redis: `127.0.0.1:6381` only
